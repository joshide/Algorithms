package revision3;

public class TopologicalSort {

	public static void main(String[] args) {
		// Topological sort represents the linear ordering of the directed acyclic graph
		// Do the depth first search of the graph
		// As node gets blacked, put them in a stack
		// print the stack
		// the out present the linear order of the graph where events happened from left to right

	}

}
