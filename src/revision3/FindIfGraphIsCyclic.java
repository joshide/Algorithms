package revision3;

public class FindIfGraphIsCyclic {

	public static void main(String[] args) {
		// Do a depth first search
		// Whenever a node is going to depth first search put that in HashSet
		// if the node already exists in HashSet that means there is cycle in the graph
		// Exit the loop when cycle found

	}

}
