package Revision2;

public class TopologicalSort {

	public static void main(String[] args) {
		// Topological sort represents the linear ordering of the graph nodes
		// Since this is linear ordering it is only for Directed Acyclic Graphs
		// In this linear ordering the event on the left happens before the event on the right
		// Do a depth first search
		// As the graph nodes blacked out, put them into the stack
		// Print the stack
		// it will print the nodes in the order that node printed first happened before node printed next
	}

}
