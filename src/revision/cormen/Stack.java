package revision.cormen;

public class Stack {

	public static void main(String[] args) {
		/*
		 * Stack is the data structure which follows LIFO policy
		 * We can use array to build the stack
		 * In this approach, we take help of the variable "top" which represent the top most element of the stack
		 * if top == -1, stack is empty
		 * if top == arr.length - 1, stack is full
		 */

	}

}
