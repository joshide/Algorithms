package geeksforgeeks.arrays;

public class TwoElementsSumClosestToZero {

	public static void main(String[] args) {
		// Sort the element
		// put two indexes , left and right: left = 0 and right = arr.length - 1
		// sum = arr[left] + arr[right];
		// keep track of absolute minimum
		// if sum > 0, right --
		// if sum < 0, left ++

	}

}
